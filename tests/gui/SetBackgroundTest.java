/*package gui;

import sys.GUITest;

public class SetBackgroundTest extends GUITest {
	@org.junit.Test
	@Override
	public void Test() {
		// TODO: use windowtester|
		// bot.menu("Video").menu("Source").menu("Camera").click();
		// TODO: use windowtester| bot.button("Start Stream").click();
		sleep(2000);

		System.out.println("setting background..");
		// TODO: use windowtester| bot.button("Set Background").click();
		sleep(1000);

		// TODO: use windowtester| int[] bgData = (int[])
		// ReflectUtils.getField(VideoManager.class,
		// PManager.getDefault().getVideoManager(), "bg_image_rgb");

		// TODO: use windowtester| assert
		// (bgData[100]!=0):"Background is Null!";
		sleep(1000);
	}

}*/